general_settings:
  port: 4000

model_list:
  # Tier 1: Local CLI Wrapper (Zero API Cost)
  - model_name: claude-3-5-sonnet
    litellm_params:
      model: openai/claude-3-5-sonnet-20240620
      api_base: http://localhost:8000/v1
      api_key: dummy-key-not-needed

  # Map Claude Code's default request directly to your Tier 1 wrapper
  - model_name: claude-sonnet-5
    litellm_params:
      model: openai/claude-3-5-sonnet-20240620
      api_base: http://localhost:8000/v1
      api_key: dummy-key-not-needed

  # Tier 2: Groq GPT-OSS 20B (Always-Free Tier)
  - model_name: fallback-groq
    litellm_params:
      model: groq/openai/gpt-oss-20b
      api_key: os.environ/GROQ_API_KEY

  # Tier 3: OpenRouter Auto-Free (Free Tier)
  - model_name: fallback-openrouter
    litellm_params:
      model: openrouter/openrouter/free
      api_key: os.environ/OPENROUTER_API_KEY

# LiteLLM Fallback Chain
litellm_settings:
  fallbacks:
    - {"claude-3-5-sonnet": ["fallback-groq", "fallback-openrouter"]}
    - {"claude-sonnet-5": ["fallback-groq", "fallback-openrouter"]}